<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/header') %>
    <title><%= blog.title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #CDE8E5;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #333;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }
        .blog-title {
            font-size: 2em;
            margin-bottom: 10px;
            color: #CDE8E5;
        }
        .profile-section {
            position: fixed;
            bottom: 4px;
    left: 25px;
            display: flex;
            align-items: center;
            padding: 10px;
            background-color: #444;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .author-profile-image {
            border-radius: 50%;
            width: 50px;
            height: 50px;
            margin-right: 10px;
        }
        .author-name {
            font-size: 1.2em;
            color: #CDE8E5;
        }
        .blog-cover {
            max-width: 100%;
            height: 50vh;
            margin-bottom: 20px;
        }
        .blog-body {
            font-size: 1.2em;
            line-height: 1.6;
            color: #CDE8E5;
        }
        .back-link {
            display: inline-block;
            margin-top: 20px;
            color: #007BFF;
            text-decoration: none;
        }
        .back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %>
    <div class="profile-section">
        <img src="<%= user.profileImageUrl %>" alt="<%= user.fullName %>" class="author-profile-image">
        <div class="author-name">By <%= user.fullName %></div>
    </div>
    <div class="container">
        <h1 class="blog-title"><%= blog.title %></h1>
        <p class="blog-author">Published on <%= new Date(blog.createdAt).toLocaleDateString() %></p>
        <img src="<%= blog.coverImgUrl %>" alt="Cover Image" class="blog-cover">
        <div class="blog-body"><%= blog.body %></div>
        <a href="/" class="back-link">← Back to Home</a>
    </div>
    <%- include('./partials/scripts') %>
</body>
</html>
